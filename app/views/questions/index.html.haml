.section
  .container 
    - content_for :main_heading do
      = "Add Question"
    %h4.title.is-4
      = "Deck: #{@deck.title}"
- if @deck.questions.any?
  .section
    .container
      %ul
        - @deck.questions.each do |question|
          %li
            = question.text
- else
  %p This deck has no questions
.section
  .container
    = link_to "Add Question", new_deck_question_path, class: "button is-primary"

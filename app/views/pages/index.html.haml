.section
  .container
    - if user_signed_in?
      - content_for :main_heading do
        = "Dashboard"
      %p Welcome Back, #{current_user.first_name} #{current_user.last_name}!
      .is-divider
      .section
        .container
          %h3.title.is-3 Decks
          %p.content Decks are reusable sets of questions. 
          %p.content 
            = link_to "Manage Decks", decks_path, class: "button is-primary"
          %h3.title.is-3 Quizzes
          %p.content Your quizzes are composed decks. Add various decks to have a quiz with different topics.
          %p.content
            = link_to "Manage Quizzes", quizzes_path, class: "button is-primary"

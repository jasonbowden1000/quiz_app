.section
  .container
    - if user_signed_in?
      %p Welcome Back, #{current_user.first_name} #{current_user.last_name}!
      .is-divider
      %h2.title.is-2 Dashboard
      .is-divider
      .section
        .container
          %h3.title.is-3 Decks
          %p.content Decks are reusable sets of questions. Quizzes are composed of decks; decks can be reused in multiple quizzes.
          %p.content 
            = link_to "Manage Decks", decks_path, class: "button is-primary"